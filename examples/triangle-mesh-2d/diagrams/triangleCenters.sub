-- create a triangle with vertices i, j, k
Vertex i, j, k
Triangle ijk := MakeTriangle( i, j, k )

-- draw three centers of the triangle
Point a, b, c
a := Barycenter(ijk)
b := Circumcenter(ijk)
c := Incenter(ijk)

AutoLabel All
