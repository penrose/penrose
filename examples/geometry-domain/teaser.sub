Point p, q, r, s

-- TODO: can't we declare and bind?

Segment a
a := MkSegment(p, q)
Segment b
b := MkSegment(p, r)
Point m
m := Midpoint(a)
Angle theta
theta := AngleBetween(a, b)
Triangle t
t := MkTriangleP(p, r, s)

-- Ray w
-- w := Bisector(theta)

-- Ray h
-- h := PerpendicularBisector(a)

AutoLabel All