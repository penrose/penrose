SimplicialComplex K

Vertex v1 ∈ K
Vertex v2 ∈ K
-- These might be the same; Style may fail if these randomly end up being the same vertex

SimplicialSet S
SubsetOf(S, K)
S := {{v1}, {v2}}

Result(S)

AutoLabel All
