Set u {
    u.shape = Circle { }
    u.shape.endX = u.startX + 2^6 + len(u.shape)
    u.shape.endX = len(1 + len(2))
    u.shape.endX = len(1 + len(2)) + len(u.shape)
    u.shape.endX = len(1 + len(2)) + 1
    u.shape.endX = len(v.shape) + u.shape.startX
    u.shape.endX = len(u.shape) + 1.0
}
