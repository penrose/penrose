/*
Reals `X` {
    X.shape = DoubleArrow {
        start = global.origin - (100, 100)
        end = global.right
        style = "solid"
        text = Auto
    }

    encourage near(D.text, D.shape.end)
}
*/
