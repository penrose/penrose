global {
    constraint at(originl, 0, 0)
    constraint at(horizl, 200, 0)
}

Set `A` {
    len = Arrow {
    start = originl
    end   = horizl
    style = "length"
    text = "|A|"
  }

 
}

Point p {
    shape = Dot {
        text = None
    }
}

