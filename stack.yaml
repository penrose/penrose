flags: {}
extra-package-dbs: []
packages:
- .
extra-deps:
- pretty-terminal-0.1.0.0

resolver: lts-13.24
allow-newer: true
ghc-options:
  "$everything": -threaded -static -O2 -optl-static
cc-options:
  "$everything": -static
ld-options:
  "$everything": -static -pthread
