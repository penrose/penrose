-- Tree.sty
Set x { 
    shape = Text { text = None }
}

Subset x y {
    objective above(y, x)
    objective sameX(x, y)
    shape =  Arrow {
        start = x.shape
        end   = y.shape
        text  = None
    }
}

Set x, Set y {
    objective repel(x, y)
}

Subset x S, Subset y S {
    objective sameHeight(x, y)
}
