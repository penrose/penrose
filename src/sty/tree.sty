-- Tree.sty
Set x {
    x.shape = Text { string = x.label }
}

Set x; Set y
where IsSubset(x, y) {
    -- objective above(y, x)
    -- objective equal(x.shape.x, y.shape.y)
    x.arrow = Arrow { }
    x.centerFn = encourage centerArrow(x.arrow, x.shape, y.shape)
}

Set x; Set y {
    -- objective repel(x, y)
}

Set x; Set y
where IsSubset(x, S); IsSubset(y, S)
with Set S
{
    -- objective sameHeight(x, y)
}
