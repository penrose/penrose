Point `A` {
  shape = Dot {}
}

Set A {
  shape = Circle {
    -- green
    color = computeColorRGBA(0.1, 1.0, 0.2, 0.4)
  }
  constraint contains(A, A.label)
}

In x y {
    constraint contains(y, x)
}
