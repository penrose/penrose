Set x {
    x.shape = Circle {
        stroke-width = 0
    }

    x.text = Text {
      string = x.label
    }
    
    x.labelFn = ensure contains(x.shape, x.text)
}

Set x; Set y
where IsSubset(x, y) {
    x.containFn = ensure contains(y.shape, x.shape)
    x.sizeFn    = ensure smallerThan(x.shape, y.shape)
    x.outsideFn = ensure outsideOf(y.text, x.shape)
}

Set x; Set y
where NotIntersecting(x, y) {
    x.notIntersectFn = ensure nonOverlapping(x.shape, y.shape)
}
