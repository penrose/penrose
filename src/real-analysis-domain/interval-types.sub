-- Set Z

Reals R
Real a, b, c, d

Interval A
A := CreateInterval(a, b)
Subset(A, R)

ClosedInterval C
C := CreateClosedInterval(c, d)
Subset(C, R)

Real e, f
OpenInterval D
D := CreateOpenInterval(e, f)
Subset(D, R)

AutoLabel All
Label R $\mathbb{R}$
