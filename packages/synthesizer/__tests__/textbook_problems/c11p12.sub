-- TODO full example is finnicky, often results in a circle with radius 0
Point A, B, C, D, E, F, d, b
Circle c := MkCircleR(F, C)
Segment AC := Chord(c, A, C)
Segment EC := Chord(c, E, C)
Segment FB := Radius(c, B)
Segment FD := Radius(c, D)
Segment FC := Radius(c, C)
Midpoint(FD, d)
Midpoint(FB, b)
-- Collinear(E, b, C)
-- Collinear(A, d, C)
Segment EA := Diameter(c, E, A)
Angle FbC := InteriorAngle(F, b, C)
Angle FdC := InteriorAngle(F, d, C)
RightMarked(FbC)
RightMarked(FdC)
AutoLabel All