-- variation: PlateauHedgehog6931

Vertex a0, a1, a2
Vertex b0, b1, b2
Triangle s, t
StandardTriangle sigma
Map f0, f1

s := MakeTriangle(a0,a1,a2)
t := MakeTriangle(b0,b1,b2)
f0 := TriangleEmbedding(sigma,s)
f1 := TriangleEmbedding(sigma,t)

Label a0 $\mathbf{f}_i$
Label a1 $\mathbf{f}_j$
Label a2 $\mathbf{f}_k$
Label b0 $\tilde{\mathbf{f}}_i$
Label b1 $\tilde{\mathbf{f}}_j$
Label b2 $\tilde{\mathbf{f}}_k$
Label f0 $\mathbf{f}$
Label f1 $\tilde{\mathbf{f}}$

