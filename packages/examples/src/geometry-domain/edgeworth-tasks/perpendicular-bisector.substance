Point A, B, C
Let AC := Segment(A, C)
Let AB := Segment(A, B)
Let BC := Segment(B, C)
Let BA := Segment(C, A)
Point M
Midpoint(BC, M)
Angle aAMC := InteriorAngle(A, M, C)
RightMarked(aAMC)
Let BM := Segment(B, M)
Let CM := Segment(C, M)
EqualLengthMarker(BM, CM)
AutoLabel A, B, C
