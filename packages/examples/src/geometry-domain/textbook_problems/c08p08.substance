Point P, Q, R, S, T
Let tPQR := Triangle(P, Q, R)
Let tQTR := Triangle(Q, T, R)
Let tTSR := Triangle(T, S, R)
Angle PQR := InteriorAngle(P, Q, R)
Angle QTR := InteriorAngle(Q, T, R)
Angle TSR := InteriorAngle(T, S, R)
Let PR := Segment(P, R)
Let QR := Segment(Q, R)
On(T, PR)
On(S, QR)
RightMarked(PQR)
RightMarked(QTR)
RightMarked(TSR)

AutoLabel P, Q, R, S, T