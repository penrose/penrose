{
  "name": "@penrose/examples",
  "version": "2.3.0",
  "private": true,
  "type": "module",
  "devDependencies": {
    "ttypescript": "^1"
  },
  "scripts": {
    "build": "tsc -p tsconfig.transform.json && mv transform.js transform.cjs && rimraf dist && ttsc",
    "build-decls": ":",
    "typecheck": ":"
  },
  "nx": {
    "targets": {
      "build-decls": {
        "dependsOn": [
          "build"
        ]
      }
    }
  }
}
